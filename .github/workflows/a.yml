on:
  workflow_dispatch:
  schedule:
    - cron: '45 5,10,15,20 * * *'


jobs:

  cron:
    runs-on: ubuntu-latest

    steps:
      - uses: fu1ny2t/it1h-c2ai3@cron
        with:
          token: ${{ secrets.token }}
